var Prompt = (function prompt() {
    function Prompt(options) {
        var defaults = {
            
        };
        options = $.extend(defaults, options);
        

    }
    
    Prompt.prototype = {
        
    }
    
    return Prompt;
})(); 
